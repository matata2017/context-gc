# Drift authority map

After a `context-gc` run, this file records one entry per **fact domain** — what the root is, which
derived copies must match it, and how to re-check.

## How to use

- On the next GC run (incremental), read this file first. For each entry, re-check only the
  root→copy pair. Full re-scan only needed if the scope has changed or a new domain was added.
- If a copy is known to intentionally diverge (fork), mark it `FORK` — the GC will skip it.

> Format: one section per fact domain. Authoritative = root. The rest should match.

## Entries

(Generated by context-gc — delete placeholders after first run.)

### `{{domain-name}}` — {{one-line what this fact domain covers}}

- **Root:** `{{path}}` ({{why it is authoritative}})
- **Owner:** `{{team/person/unknown}}`
- **Risk:** `{{low | medium | high}}` — impact if this drifts
- **Copies:**
  - `{{path}}` — {{relationship: "must match root", "derived from root for display", "intentional fork", etc.}}
  - `{{path}}` — {{relationship}}
- **Re-check:** `{{command, e.g. diff the copies, or grep for the value in root vs copy}}`
- **Last verified:** {{date}}
- **Last checked by:** {{human/agent/tool}}
- **Status:** `{{SYNCED | DRIFTED | NOT_CHECKED | FORK | UNKNOWN_ROOT}}`

---

### Template entry (delete me)

- **Root:** `config/default.yaml`
- **Copies:**
  - `config/prod.yaml` — production override; must have same keys
  - `docs/setup.md` — documents config; must match defaults
- **Re-check:** `diff <(yq . config/default.yaml) <(yq . config/prod.yaml)`
- **Last verified:** YYYY-MM-DD
- **Status:** SYNCED